{
  "main": "../bindings/node/gdml",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tree-sitter generate",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground"
  }
}
